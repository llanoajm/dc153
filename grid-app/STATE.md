# Steinmetz — current state

Build cursor. Update when you finish something or change direction. Last updated: 2026-05-26.

## What's wired (works today)

- **Frontend (`/home/agent/grid-app`)**: Next.js 16, Tailwind 4, Supabase Auth.
  - Landing `/` — Steinmetz lockup, sign-in / create-account.
  - `/login`, `/signup` — email+password forms.
  - `/auth/callback`, `/auth/signout` — route handlers.
  - `/app` — authed area. Materializes the user's workspace on first visit. Bare v0 chat: user message in, polls for assistant response, only text parts visible.
  - `/demos` — **public** (not auth-gated) video walkthroughs of every validated flow from
    Phase F.10. Recordings produced by `npx playwright test --config=playwright.demos.config.ts`
    and served from `public/demos/<slug>.webm`; manifest in `lib/demos.ts`.
    Sits outside `app/app/` so neither `proxy.ts`'s `/app/**` matcher nor the
    layout's session redirect applies; reachable from the LeftRail "Demos" entry.
  - `proxy.ts` — Next 16 proxy gating `/app/*` and `/api/opencode/*` on a Supabase session.
- **Backend services on the VM**:
  - opencode server (our fork) on `127.0.0.1:4096`, OpenRouter key in env, persistent SQLite at `~/.local/share/opencode/`.
  - Next.js dev on `0.0.0.0:3000`.
  - Both started detached (`nohup ... & disown`); verify with `ss -tlnp | grep -E ':3000|:4096'`.
- **Per-user workspace** at `/home/agent/grid-workspaces/<supabase-user-id>/`:
  - Auto-materialized in `lib/user-workspace.ts` on first `/app` request.
  - Contains `AGENTS.md` (workspace conventions), `.opencode/agent/grid-engineer.md` (primary agent persona), empty `features/` + `.opencode/skills/`.
- **zap library** at `/home/agent/zap`, installed editable in `.venv`. Treated as read-only by end-user-mode agents.
- **Supabase**: project `hfqkimojlihvdesokkqr`. Auth on. Schema at `supabase/schema.sql` (also served at `/schema.sql`). **User has been instructed to paste the schema into the SQL editor**; verify with the user before relying on `profiles` / `features` tables.
- **Branding**: matches `llanoajm/steinmetz-web/index.html`. Sora + EB Garamond, black-on-white, centered.

## What is NOT wired (deferred)

Everything past v0. See `ROADMAP.md` build order for the prioritized list. Highest-impact gaps:

- ~~**Tool-call cards in chat**~~ Done (2026-05-23): cards for Read, Edit, Write, Bash, Grep, Glob, WebFetch, WebSearch, Skill in `components/chat/cards/` + dispatcher at `components/chat/ToolCallCard.tsx`; unknown tools fall back to a generic JSON card. Reasoning parts collapse behind a `<details>`.
- ~~**Workspace shell**~~ Done (2026-05-23): `components/shell/{WorkspaceShell,LeftRail,CenterTabs,RightRail,CommandPalette}.tsx` mounted in `app/app/layout.tsx`. Left rail has the default sections (Chats, Sources, Networks, Datasets, Runs, Reports, Skills/Features, Glossary); center pane is a tab bar with Chat as the default (closable=false) tab; right rail is contextual/placeholder, collapsed by default; Cmd/Ctrl+K opens an empty palette. Rail items don't route yet — they just track active state in-memory.
- ~~**Artifacts table + universal renderer**~~ Done (2026-05-23): `artifacts` table in `supabase/schema.sql` with RLS (own-rows + canonical-shared). `lib/artifacts.ts` exposes `createArtifact`/`getArtifact`/`listArtifacts`. `components/renderers/{markdown,table,chart,diff,code,file,log,dashboard}.tsx` exist; dispatch via `components/renderers/index.tsx`. `/api/artifacts` GET/POST + `/app/artifacts/[id]` view page. **User must paste updated `supabase/schema.sql` into the Supabase SQL editor** before the table is reachable.
- ~~**Streaming** — current chat polls every 2.5s during pending state.~~ Done (2026-05-23): SSE proxy at `app/api/opencode/session/[id]/stream/route.ts` filtered to the session, chat consumes via EventSource (no polling).
- **Per-user MCP server exposing `features/`** — agent re-reads its own code each session.
- ~~**Bundled reference networks**~~ Done (2026-05-23): `data/networks/{ieee-30,pypsa-usa,pypsa-eur-slice}/` ship as PyPSA CSV folders with per-network `card.md`. `scripts/smoke_dispatch.py <dir>` solves a 1-hour dispatch via zap (HIGHS → CLARABEL → SCS fallback) and `scripts/seed_networks.py` upserts canonical (`user_id=null`, `status='canonical'`) rows into `public.artifacts`. `scripts/build_networks.py` regenerates the folders from MATPOWER + `pypsa.examples.scigrid_de`.
- ~~**Geo-map renderer**~~ Done (2026-05-23): `components/renderers/geo-map.tsx` is a `"use client"` component that lazy-loads `maplibre-gl` + CSS via dynamic import (kept out of pages that don't mount it). OpenStreetMap raster tiles (no token required). Bus glyphs by lat/lon when present; force-directed SVG fallback otherwise. Lines color by signed flow, thickness log-scaled by `s_nom`, dashed when DC. Per-bus LMP colors on circle paint; hover popup shows id / carrier / LMP. Time slider scrubs `dispatch.hours`; both LMPs and flows re-paint on scrub. Selected via `view_spec.renderer = "geo-map"` (default for `kind='network'` is still the topology graph).
- ~~**Source document ingestion (PDF → glossary / company-context)**~~ Done (2026-05-23): `app/api/upload/pdf/route.ts` accepts a PDF, writes it under `<workspace>/sources/<slug>/v<N>/<file>.pdf`, creates a `source_document` artifact, and spawns `scripts/ingest_pdf.py` detached. The script extracts text via `pypdf`, chunks into ~1200-char pieces with overlap, computes deterministic hashing-based bag-of-words embeddings (128-dim, L2-normalized), inserts into `public.source_chunks` (new table with RLS), merges extracted terms into `<workspace>/glossary.md` and a narrative blurb into `<workspace>/company-context.md`. Both files are listed in opencode's `instructions:` config (`.opencode/opencode.jsonc`) so they auto-load into every session's system prompt (verified by reading opencode's `session/instruction.ts`). Re-uploading with the same slug bumps `metadata.version` and chains `parent_id`; the script emits a `diff` view spec showing old vs. new extracted text. New UI: `/app/sources` (drag-drop panel) and `/app/glossary` (live side-by-side view of `glossary.md` + `company-context.md`). PPTX/images/audio remain TODO (roadmap item 13).
- ~~**Web-fetch data acquisition**~~ Done (2026-05-23): `/api/fetch` and the MCP tool `steinmetz__fetch_network` download a URL into `<workspace>/sources/<slug>/raw/`, write a `network` artifact with `metadata.source_url` / `.license` / `.fetched_at` / `.checksum`, and re-use the upload ingestion pipeline. Missing-license keeps the artifact `status='draft'` with `metadata.license_unknown=true`; the workspace AGENTS.md tells the agent to prompt the user before promoting to canonical.

## Hardening flags (opt-in)

- **`STEINMETZ_ENABLE_LINUX_ACCOUNTS=1`** — engages HARDENING §3.1 (per-user
  Linux accounts). Default off because chmod 700 + foreign owner on the
  workspace would block grid-app's existing org-overlay / provider-key /
  source-upload writes until §3.2 (per-user opencode units) ships. Flip the
  flag in production once §3.2 lands.
  - **Host prereqs** before flipping on:
    - `sudo groupadd --system steinmetz`
    - `sudo usermod -aG steinmetz agent` (so grid-app sees the `steinmetz`
      group on its supplementary GID list; the dir entry remains stat-able
      via x on `/home/agent/grid-workspaces`).
    - `/etc/sudoers.d/steinmetz-hardening` granting the `agent` user
      NOPASSWD on the exact paths used by `lib/linux-account.ts`:
      ```
      agent ALL=(root) NOPASSWD: /usr/sbin/useradd, /bin/chown, /bin/chmod
      ```
      Tighten to argv globs (`/usr/sbin/useradd --system --no-create-home --home /home/agent/grid-workspaces/* steinmetz-*` etc.) once the production deploy script is in place.
  - **What the flag does** on each `ensureUserWorkspace(userId)`:
    1. mints `steinmetz-<short-uid>` (sha256(userId) → 16 hex chars; full
       username fits the 32-char Linux cap).
    2. `useradd --system --no-create-home --home <workspace> steinmetz-<short-uid>` (exit 9 if exists → no-op).
    3. `chown -R steinmetz-<short-uid>:steinmetz <workspace>`.
    4. `chmod 700 <workspace>`.
  - **Detection on re-entry**: `ensureUserWorkspace` stats the dir first; if
    the uid is no longer `agent`, the workspace is treated as already
    provisioned and the bootstrap path is skipped (grid-app can't enter a
    700 dir owned by someone else anyway).
  - **shortUidFor / linuxUserFor / linuxGroup** are exported from
    `lib/user-workspace.ts` (re-exported from `lib/linux-account.ts`) for
    items 3.2 (systemd template parameter) and 3.3 (per-user venv path).

- **`STEINMETZ_PER_USER_OPENCODE=1`** — engages HARDENING §3.2 (per-user
  opencode server). Default off because §3.1's chmod 700 + per-user Linux
  account must be live first; without it the systemd unit
  `steinmetz-opencode@<short-uid>` has no user to drop privileges to. Flip
  on (alongside `STEINMETZ_ENABLE_LINUX_ACCOUNTS=1`) once both items are
  ready in production.
  - **Host prereqs** before flipping on (see `infra/systemd/README.md`):
    - `sudo cp infra/systemd/steinmetz-opencode@.service /etc/systemd/system/ && sudo systemctl daemon-reload`
    - `/etc/sudoers.d/steinmetz-opencode` granting the `agent` user
      NOPASSWD on `/usr/bin/systemctl start steinmetz-opencode@*.service`
      (and `... status ...` for diagnostics).
    - Optional `/etc/steinmetz/opencode.env` (root:steinmetz 0640) for
      the fallback OpenRouter key + other shared opencode env.
  - **What the flag does** on each opencode request:
    1. `lib/opencode-transport.ts:ensurePerUserOpencode(userId)` issues
       `sudo systemctl start steinmetz-opencode@<short-uid>.service`
       (idempotent — no-op when already active).
    2. Waits up to `STEINMETZ_OPENCODE_SOCKET_WAIT_MS` (default 15 s)
       for `/run/steinmetz/<short-uid>.sock` to appear.
    3. Dials the socket via `node:http` (Unix-socket transport) instead
       of the §1.2 TCP fronting proxy. Filesystem perms (0660 socket
       owned by `steinmetz-<short-uid>:steinmetz`) are the auth layer.
  - **Per-user supervisor**: `scripts/per-user-opencode.ts` runs under the
    systemd template. Spawns opencode on a deterministic loopback port
    derived from the short-uid (range 41000-60999) and forwards bytes
    between the socket and that port.
  - **Idle eviction**: the supervisor exits 0 after
    `STEINMETZ_OPENCODE_IDLE_TIMEOUT` seconds (default 1800 = 30 min) of
    no socket activity. `Restart=on-failure` leaves the unit stopped; the
    next request boots it back up.
  - **§1.2 fronting proxy**: still required for the shared-opencode
    fallback. Under `STEINMETZ_PER_USER_OPENCODE=1` it additionally
    accepts an `X-Steinmetz-Short-Uid` header and forwards to the matching
    per-user socket (Bun's `fetch({ unix })`). In-app traffic does NOT go
    through the proxy in per-user mode — grid-app talks to sockets
    directly via the transport — but external curl/debug clients can.

- **`STEINMETZ_PER_USER_SLICES=1`** — engages HARDENING §3.4 (per-user
  cgroup slices). Default off because the slice unit only does something
  when §3.2's per-user opencode unit is also running (the unit declares
  `Slice=steinmetz-%i.slice`); without a backing opencode service the
  slice is just inert. Flip on alongside `STEINMETZ_ENABLE_LINUX_ACCOUNTS=1`
  and `STEINMETZ_PER_USER_OPENCODE=1` in production.
  - **Host prereqs** before flipping on (see `infra/systemd/README.md`
    Step 7):
    - `/etc/sudoers.d/steinmetz-hardening` (mode 0440) grants the `agent`
      user NOPASSWD on the two commands `ensureUserSlice` runs:
      ```
      agent ALL=(root) NOPASSWD: /usr/bin/install -m 0644 -o root -g root /tmp/* /etc/systemd/system/steinmetz-*.slice
      agent ALL=(root) NOPASSWD: /usr/bin/systemctl daemon-reload
      ```
  - **What the flag does** on each `ensureUserWorkspace(userId)`:
    1. Reads the in-repo template `infra/systemd/steinmetz-%i.slice`.
    2. Substitutes `%i` for the short-uid and rewrites
       `MemoryMax=`/`CPUQuota=`/`IOWeight=` per the user's resolved
       compute tier (`profiles.compute_tier`; null → "default").
    3. `sudo install` the rendered body to
       `/etc/systemd/system/steinmetz-<short>.slice`, then `sudo
       systemctl daemon-reload`.
  - **Tier resolver**: `lib/compute-tier.ts` exports `COMPUTE_TIERS`
    (`low` / `default` / `high`) + `resolveComputeTier(tier?)`. Default
    tier mirrors the slice template defaults: 4G mem / 200% CPU /
    IOWeight=100. Unknown tier strings fall back to "default" rather
    than fail.
  - **Schema**: `supabase/schema.sql` adds `profiles.compute_tier text`
    (nullable). User has to paste the updated schema into Supabase
    before the column is reachable; until then everyone resolves to
    "default" (no error — the resolver treats null as default).
  - **Failure mode**: if the flag is off, sudo is missing, or `install`
    fails, the slice file is absent. `systemctl start steinmetz-opencode@`
    still works but systemd creates a transient slice with no resource
    caps — observable as `Loaded: not-found` on `systemctl status
    steinmetz-<short>.slice`. The chat keeps working; resource
    isolation is what's lost.

## Known operational gotchas

- The OpenRouter key was pasted in chat earlier; **user should rotate it**. Same for the Supabase service-role key.
- Background processes I started via `nohup ... & disown` survive my agent session, but **they don't survive a VM reboot**. If the user reboots, restart with the Quick Start commands in `AGENTS.md`.
- `proxy.ts` matcher excludes images and most static assets, but if you add a new public file type, double-check it doesn't get caught by the auth gate.
- Supabase: if email confirmation is enabled in their Auth settings, signup→`/app` is a two-step flow (signup → check email → click link → `/app`). For local dev they should disable it.
- The default model on chat is `~anthropic/claude-sonnet-latest` via OpenRouter, default agent is `grid-engineer`. Hardcoded in `lib/opencode-client.ts`. Move to user-settable when adding settings UI.

## Decisions made during build

- **Chose Next.js 16** (was released; we hit some new-version breakages, see `node_modules/next/dist/docs/01-app/02-guides/upgrading/version-16.md`).
- **Hard fork zap to `llanoajm/zap`** (committed `.opencode/` bootstrap, reset main to that bootstrap; the M2 nitrogen-emissions demo commit lives on branch `demo/m2-maintainer-mode-test` for reference).
- **Reverted the "agent commits to zap" model** after user clarified per-user isolation. End-user features go to per-user workspace + Supabase, never to the zap repo.
- **Chose OpenRouter** as inference provider (user's key, no opencode.ai hosted layer).
- **Picked port 3000** for Next.js (we killed the legacy `packages/app` from the opencode monorepo earlier).

## What to do next (suggested)

Per ROADMAP build order, item #17: **Agentic view authoring v2 — sandboxed
custom panels**. Items #1–#16 shipped on 2026-05-23.

Alternative starting points if user wants something else:
- Wire the rail sections to actual routes / panels before shipping more backbone work.
- Replace the ACTIVSg200 stand-in under `data/networks/pypsa-usa/` with a
  real PyPSA-USA snapshot once the upload pipeline lands.

## Agent log

- 2026-05-26 — demos surface landed (GPU_PARITY_ROADMAP §Phase F.99, LOOP_QUEUE item 99 — terminal item). New page `app/app/demos/page.tsx` lists Playwright-captured `.webm` walkthroughs of every validated flow from Phase F.10. Recording wiring: `playwright.demos.config.ts` (separate from the regular suite — `video: { mode: "on", size: 1024×700 }`, `outputDir: ./tests/demos/_artifacts`, projects `demos-anon` for `.anon.demo.ts` and `demos-auth` for `.demo.ts`); 13 specs in `tests/demos/01..13-<flow>.demo.ts`; shared helpers `tests/demos/_helpers.ts` (`saveVideoAs(page, slug)` copies the clip into `public/demos/<slug>.webm` after `page.close()` flushes the codec). One recording per validated flow: `signed-out-redirect` / `login` / `app-shell` / `chat-input` / `chat-offline-retry` (the 10.2 fix) / `networks` / `rail-nav` / `dashboards` / `features` / `glossary` / `orgs` / `settings` / `logout`. Manifest in `lib/demos.ts` (`DEMO_CLIPS` array + `listExistingDemos()` which only surfaces clips whose .webm is on disk and >0 bytes; missing clips appear under a separate "Awaiting recording" section so the page degrades gracefully). Page renders title / flow tag / description / inline `<video controls preload="metadata" playsInline>` per available clip. New LeftRail entry "Demos" routes via `RAIL_ROUTES.demos` → `/app/demos`; `WorkspaceShell` learned the active-rail mapping and tab label. Verification: 13/13 demo recordings exit 0 (54s total); total clip size 1.3MB (well under the ~20MB `public/` budget — staying in-repo); `tests/flows/demos-page.spec.ts` exercises the logged-in page (mounts + every `<video>` src resolves 200); `tests/flows/rail-pages.spec.ts` gained `/app/demos` and is still 10/10 green; signed-out → `/login?next=%2Fapp%2Fdemos` redirect confirmed via `proxy.ts`'s generic `/app/**` matcher (no demos-specific branch needed). `npm run build` succeeds. Operational note: re-running the recordings requires the dev stack on :3000 + the auth-state.json that `tests/fixtures/global-setup.ts` mints from `STEINMETZ_TEST_ACCOUNT_*`; rerunning `npm run start` after `npm run build` is still required (see existing "stale Next.js chunks" gotcha).
- 2026-05-26 — zap PyPSA importer hardening landed (GPU_PARITY_ROADMAP §Phase A.2, LOOP_QUEUE item 2). Zap commit `9fd9599` (origin/main) swaps every `series.replace(buses_to_index).values.astype(int)` site for `series.map(buses_to_index).to_numpy(dtype=int)` in `parse_generators` / `parse_loads` / `get_source_sinks` / `parse_storage_units` / `parse_stores`, and rebinds `dynamic_costs` out-of-place so the cost-perturbation no longer mutates a read-only ndarray (pandas 3.0 Copy-on-Write). New `zap/tests/test_pypsa_importer_string_buses.py` exercises raw read-only `.values` semantics (bypasses the test-suite writability shim) and confirms string-typed bus columns parse correctly. The grid-app-side `scripts/_pypsa_compat.py` + `infra/modal/solver_app.py::_patch_pandas_cow_in_container` shims are STILL required because `zap.devices.injector::scale_costs`/`scale_power` continues to mutate `.values`-derived arrays in-place — fixing the injector is out of scope for this item. `python scripts/smoke_dispatch.py data/networks/ieee-30` still exits 0 on the CPU path (no importer regression).
- 2026-05-23 — agentic view authoring v1 landed (ROADMAP §11.8, LOOP_QUEUE item 16). New schema module `lib/view-specs/dashboard.ts` documents the dashboard view spec (panels list + optional `controls` + grid `columns`), exports `DashboardViewSpec` / `DashboardPanel` / `TimeSliderControl` types, `validateDashboardSpec()`, `clampSpan()`, `initialControlState()`, and `DASHBOARD_DEFAULTS`. Panels can either inline a `view_spec` or reference an existing artifact via `artifact_id` — the dashboard resolves the reference client-side and renders the referenced row's view_spec through the matching renderer. `components/renderers/dashboard.tsx` was rewritten as a `"use client"` component that dispatches to every built-in renderer (markdown, table, chart, diff, code, log, file, network-graph, geo-map, run, dashboard) — agents can now compose maps + topology + time series in one layout. Shared controls: when the spec declares `controls: [{kind:"time-slider", id, hours}]` and a panel sets `share_controls: {"<control-id>": "<view-spec-key>"}`, the dashboard writes the active index under that key on a synthetic copy of the panel's view_spec, plus the always-present `_controlled_hour_idx`. `components/renderers/geo-map.tsx` honours `_controlled_hour_idx` by skipping its own slider and locking to the parent value. `components/runs/RunView.tsx` reads the same key and overlays a dashed Vega-Lite rule layer at the active timestamp on each of the LMP / carrier / flow charts so chart panels visibly track the shared slider too. Re-rendering is pure-functional from `view_spec` (no hidden runtime state), so a saved dashboard reproduces identically. Pinning: `metadata.pinned = true` on any `kind in ('dashboard','view')` artifact promotes it to the workspace rail; toggle endpoint `POST /api/artifacts/[id]/pin` (records audit), generic single-row routes `GET/PATCH /api/artifacts/[id]/route.ts`, server helper `lib/dashboards.ts` exposes `listDashboards()` + `listPinnedDashboards()` + `isDashboardArtifact()` + `isPinned()`. `app/app/layout.tsx` fetches pinned rows server-side and threads them through `WorkspaceShell → LeftRail` (new shared type `components/shell/types.ts:PinnedDashboard` avoids a client-module cycle). The rail now renders a "Pinned" section below the default sections; selecting an entry routes to `/app/artifacts/<id>` and highlights via the synthetic key `pin:<id>`. `/app/dashboards` lists every dashboard the caller can see with a per-row Pin button (`components/dashboards/PinButton.tsx` — optimistic toggle + `router.refresh()` so the layout's pinned list reseeds). The artifact view page (`app/app/artifacts/[id]/page.tsx`) shows the Pin button in its header for any dashboard artifact. LeftRail gained a "Dashboards" rail item; `WorkspaceShell` maps it to `/app/dashboards` and learns the corresponding tab label / active rail key. `npm run build` exits 0. **Caveats**: only geo-map and run currently read the time control overlay — chart/table/network-graph panels render fine but ignore the slider (they don't have a time dimension that's actionable yet); `share_controls` is namespaced per-control so future controls (carrier filter, scenario selector) can slot in without churning panel authors; pinned list is capped at 20 entries; pin state lives on artifact `metadata`, so updating other metadata fields via PATCH must round-trip the existing metadata (the pin endpoint does this correctly).
- 2026-05-23 — org scopes + shared glossary/skills/networks landed (ROADMAP §10, LOOP_QUEUE item 14). New tables `public.orgs(id, name, slug unique, created_at, created_by)` and `public.org_members(org_id, user_id, role check 'owner|admin|member', primary key (org_id, user_id))`. RLS uses two SECURITY DEFINER helpers — `public.is_org_member(uuid)` and `public.has_org_role(uuid, text[])` — to avoid recursive policy chains on `org_members`. `public.create_org(name, slug)` is the atomic owner-bootstrap RPC (grants execute to `authenticated`). Artifacts RLS rewritten: select admits personal (`auth.uid()=user_id`), bundled canonical (`user_id is null and org_id is null and status='canonical'`), and any row whose `org_id` is in the caller's memberships; insert requires self-author + (no org OR member of org); update/delete by author OR org owner/admin. `lib/orgs.ts` exposes `listMyOrgs/getOrg/listOrgMembers/getMyRoleIn/createOrg/addMember/slugify` plus `syncOrgContextOverlays(workspaceDir)` which pulls every membership's canonical `glossary` + `context_doc` artifacts, writes them to disk as `glossary.org.<slug>.md` / `company-context.org.<slug>.md`, drops stale overlay files for orgs the user has left, and rewrites `.opencode/opencode.jsonc` so `instructions:` lists overlays first then personal `glossary.md` + `company-context.md` (org first, personal layered on top — verified by opencode's array-order resolution in `session/instruction.ts`). `lib/user-workspace.ts` got `writeOpencodeConfig` and `applyOrgOverlays` factored out so the org sync can rewrite the config without duplicating MCP boilerplate. `lib/artifacts.ts` + `/api/artifacts` accept `org_id` + a `scope=personal|all` query param. New routes: `GET/POST /api/orgs`, `GET/POST /api/orgs/[id]/members`, `DELETE /api/orgs/[id]/members/[userId]` (lets a member leave or an owner/admin remove), `GET/PUT /api/orgs/[id]/context` (PUT writes the org's canonical glossary or `context_doc` artifact; PUT is gated on `getMyRoleIn` being owner/admin to short-circuit before RLS). New page `/app/orgs` (`components/orgs/OrgsPanel.tsx`) — lists memberships, "create org" form, per-org panel with member list + email invite + side-by-side textarea editors for `org glossary` / `org company-context` (disabled for plain members). `LeftRail` got an "Orgs" entry routing to `/app/orgs`; `WorkspaceShell` learned the corresponding tab label and active-rail mapping. `/app/glossary` now calls `syncOrgContextOverlays` on render and stacks an "From org: <name>" pane for each membership above the personal panes. `proxy.ts` matcher now also gates `/api/orgs/**`. Member invite resolves email → `user_id` by reading `public.profiles` with the service-role client so the inviter never needs cross-user read perms on profiles. Build passes. **User must paste updated `supabase/schema.sql` into Supabase** before any of the new tables/RPCs/policies are live; the email-based invite also depends on the existing `public.profiles` row being populated by the `handle_new_user` trigger (already in the schema). **Caveats**: there's no invitation/notification flow yet — adding a member is a direct insert, so the invitee just sees the new org the next time they hit `/app`; member display uses raw `user_id` (we don't expose other members' emails through RLS — needs a server-side join via service role if the org owner wants email-keyed UI); org canonical features aren't separately editable in this PR — the existing Features panel surfaces them via the artifacts RLS scope but there's no "promote to org" affordance yet (next iteration); the overlay-file diffing on `applyOrgOverlays` is best-effort — if the workspace dir gets out-of-band edits the sync still wins on next /app load.
- 2026-05-23 — multi-modal source extraction landed (ROADMAP §1, LOOP_QUEUE item 13). New ingesters: `scripts/ingest_pptx.py` (slide text + per-slide images extracted via `python-pptx` + per-slide vision captions; one chunk per slide), `scripts/ingest_image.py` (single-chunk caption-only flow), `scripts/ingest_audio.py` (transcript + speaker turns, one chunk per turn). Shared helper `scripts/_caption.py` wraps OpenRouter chat completions for both vision (`anthropic/claude-sonnet-4` by default) and audio (`google/gemini-2.0-flash-001`); if `OPENROUTER_API_KEY` is missing from env + `.env.local`, both fall back to deterministic placeholder strings — image gets pixel-dimensions + mean-color, audio gets file-metadata only — so the pipeline keeps walking the queued→extracting→embedded→ready ladder regardless. Models are overrideable via `STEINMETZ_VISION_MODEL` / `STEINMETZ_AUDIO_MODEL`. New universal upload route `app/api/upload/source/route.ts` dispatches by mime+extension to the matching script (`ingest_pdf.py` / `ingest_pptx.py` / `ingest_image.py` / `ingest_audio.py`) and writes the file under `<workspace>/sources/<slug>/v<N>/`; same versioning + `parent_id` chaining as the existing PDF route, so re-uploading a slug as an updated source picks up the diff view spec. `SourcesPanel` now accepts the full supported set and renders type-aware subtitles (slides / images / turns / W×H counts). `python-pptx 1.0.2` installed into the venv via `pip --target=...` (matches the pypdf install pattern). Build passes. **Caveats**: vision/audio captions only work once `OPENROUTER_API_KEY` is added to `grid-app/.env.local` — without it, captioning is a placeholder string explaining the missing key; speaker-turn detection is a regex on `Speaker N:` line prefixes, so any model that doesn't emit that prefix collapses everything into one big chunk; PPTX picture extraction relies on `shape.image.blob`, so vector shapes / embedded charts that aren't raster get skipped (only their text frames make it into the chunk).
- 2026-05-23 — candidate feature drafting + approval flow landed (ROADMAP §4, LOOP_QUEUE item 12). New `scripts/draft_features.py` scans extracted PDF text for math-shaped sections — a heading from a small vocabulary (Objective, Cost Function, Constraint, Formulation, Mathematical Model, …) combined with at least one math signal nearby (minimize/maximize/subject-to, summation/product/integral notation, equations, comparators). For each match it writes a `<workspace>/features/<source_slug>__<heading_slug>.py` stub carrying the verbatim excerpt as a module docstring, a `_EXCERPT` constant, and two public functions (`draft_excerpt()`, `draft_heading()`) so the per-user MCP server lists the draft as a callable tool the moment it appears. `scripts/ingest_pdf.py` now calls the drafter after chunking and inserts one `feature` artifact row per draft via the service-role REST path — `status='draft'`, `parent_id=<source_document_id>`, `view_spec.renderer='code'` (so /app/artifacts/<id> renders the Python source), `metadata.heading` / `.excerpt` / `.source_artifact_id` / `.char_offset` for the lineage view. Slugs use `_` so resulting Python files import cleanly and MCP tool names stay tidy (`<source>__<heading>__draft_excerpt`). New routes: `app/api/features/route.ts` (GET; filterable by status), `app/api/features/[id]/route.ts` (PATCH with `{status?, code?}`; approve keeps `features/<slug>.py` active, reject renames it to `features/_<slug>.py` so the MCP server stops listing it, edit overwrites the file). RLS handles the multi-user gating — service-role inserts during ingestion, user-scoped supabase client for mutations. New UI: `/app/features` (`components/features/FeaturesPanel.tsx` — status filter chips, per-row approve/reject/edit with an inline textarea editor, "from <source>" link inline; `components/features/LineageStrip.tsx` mounted on /app/artifacts/[id] so opening a feature shows its source parent and opening a source shows the features it spawned + a link to /app/features). LeftRail "Skills / Features" routes via `RAIL_ROUTES.skills`; primary tab labels updated. `npm run build` exits 0. **Caveats**: the heuristic detector is intentionally conservative — short blocks with only one signal are skipped to keep the panel from getting spammed; re-ingest is idempotent (drafts whose target file already exists are skipped so user-edited code is never clobbered); rejection only hides the file from MCP, the source still lives at `_<slug>.py` so accidental rejection is recoverable; lineage is one hop deep — chains feature → source_document → (eventually) chat session aren't traversed yet.
- 2026-05-23 — streaming + tool-call cards landed. SSE proxy at `/api/opencode/session/[id]/stream` filters opencode `/event` to the requested sessionID and re-emits as SSE. Chat reducer maintains `messages: UiMessage[]` keyed by id; deltas accumulate into part fields. Login page now wraps `useSearchParams` in `<Suspense>` so `next build` passes (pre-existing issue surfaced when build was actually run).
- 2026-05-23 — workspace shell landed. `components/shell/WorkspaceShell.tsx` composes LeftRail (collapsible, default sections per ROADMAP §11.5), CenterTabs (chat as default non-closable tab; new tabs would be closable), RightRail (collapsed by default — placeholder copy), and CommandPalette (Cmd/Ctrl+K toggles a modal; empty state for now). AppLayout was switched from `<div flex-1>{children}</div>` to mounting `<WorkspaceShell>{children}</WorkspaceShell>`; added `min-h-0` so the chat's nested flex sizing keeps working. Rail nav is in-memory `active` only — no routing yet.
- 2026-05-23 — bundled reference networks landed. Three folders under `data/networks/`: `ieee-30` (30 buses, 6 generators — IEEE classic via MATPOWER case30), `pypsa-usa` (200 buses, 49 generators — Texas A&M ACTIVSg200 as US-Western stand-in until real PyPSA-USA snapshot is ingested), `pypsa-eur-slice` (585 buses, 1423 generators, 24-hour SciGRID-DE slice via `pypsa.examples.scigrid_de`). Each folder holds a PyPSA CSV export plus a `card.md` (source URL, license, scale, carrier mix, example zap solve, suggested first prompt). `scripts/build_networks.py` regenerates the folders from MATPOWER + scigrid_de; cached MATPOWER `.m` sources live in `data/_cache/`. `scripts/smoke_dispatch.py <net_dir>` loads the folder via pypsa, hands it to `zap.importers.load_pypsa_network`, and solves a 1-snapshot dispatch (HIGHS preferred, CLARABEL → SCS fallback). Smoke times: 0.13s / 0.26s / 0.64s. `scripts/seed_networks.py` runs the smoke per network and upserts canonical rows (`user_id=null`, `org_id=null`, `status='canonical'`) into `public.artifacts` using the service-role key from `.env.local`; supports `--dry-run`. Two-step check-then-insert is used instead of PostgREST `on_conflict` so we don't need a unique index on `slug`. **The seed will 404 until the user pastes the latest `supabase/schema.sql` into Supabase**, but the smoke acceptance check is fully decoupled and passes today. Workaround for a pandas-3.0 + zap clash: `scripts/_pypsa_compat.py` patches `DataFrame.values` / `Series.values` to return writable copies (zap mutates those arrays in-place with `+=` / `/=`, which Copy-on-Write rejects). Tiny MATPOWER `.m` parser at `scripts/_matpower.py`. Default solver is HIGHS because CLARABEL diverged on the SciGRID DE problem.
- 2026-05-23 — agentic data acquisition landed (ROADMAP §6.5, LOOP_QUEUE item 8). New TS endpoint `app/api/fetch/route.ts` accepts `{url, name?, slug?, license?, checksum?, session?}`, validates auth, then shells out to `scripts/fetch_url.py`. The Python helper streams the URL into `<workspace>/sources/<slug>/raw/<filename>`, computes a SHA-256, verifies it against `expected_checksum` if supplied (mismatch → 422 with `error: "checksum_mismatch"`), and writes an `artifacts` row carrying `metadata.source_url`, `metadata.license`, `metadata.fetched_at` (ISO-8601 UTC), `metadata.checksum` (formatted `sha256:<hex>`), `metadata.acquisition='web-fetch'`. License capture is enforced: empty / "unknown" / "tbd" / "n/a" → `metadata.license_unknown=true` and `status='draft'` (workspace AGENTS.md spells out that the agent must prompt the user before promoting to canonical). Validation re-uses the existing pipeline — `fetch_url.py` spawns `ingest_pypsa_folder.py` detached at the end of its synchronous portion so the converter chain (PyPSA folder → MATPOWER → custom importers) and smoke-dispatch run identically to a manual upload. The MCP server gained a `steinmetz__fetch_network` built-in that imports `fetch_url.fetch_to_workspace` directly, so the agent can call it after WebSearch/WebFetch resolves a URL. Proxy gates `/api/fetch` alongside `/api/upload`. The MCP server smoke-tests confirm the new tool appears in `tools/list` next to the existing `list_pending_imports` / `inspect_upload` / `write_custom_importer` trio.
- 2026-05-23 — heterogeneous upload + custom-importer loop landed. `scripts/ingest_pypsa_folder.py` now runs a converter chain on upload: PyPSA CSV folder → MATPOWER `.m` (via `scripts/_matpower.py` round-tripped through PyPSA) → custom importers under `<workspace>/features/import_*.py` (each exposing `matches(folder) -> bool` and `convert(folder, dest) -> None`). If none match, the script writes `sources/<slug>/inspection.json` with a SHA-1 schema fingerprint + per-CSV column lists + sample rows and flips `pipeline_status='awaiting_importer'` (status stays `draft`, not `failed_validation` — the upload isn't broken, we just don't know how to read it). Hard parse failures still go to `failed_validation`. The per-user MCP server (`scripts/user-mcp-server.py`) now ships three built-in tools alongside feature-discovered ones: `steinmetz__list_pending_imports`, `steinmetz__inspect_upload(slug)`, `steinmetz__write_custom_importer(slug, python_source, skill_markdown)`. `lib/user-workspace.ts` passes `STEINMETZ_WORKSPACE_DIR` to the MCP server and the workspace AGENTS.md documents the importer convention end-to-end. New route `app/api/upload/reingest/[id]/route.ts` resets a `network` artifact to `queued` and re-spawns the detached ingester so the user (or agent) can re-trigger the pipeline after authoring an importer. Subsequent uploads of the same schema are handled automatically — `_try_custom_importers` iterates `features/import_*.py` and the first whose `matches()` returns True wins.
- 2026-05-23 — geo-map renderer landed (ROADMAP §11.7, LOOP_QUEUE item 9). Added `maplibre-gl@^5.24` and authored `components/renderers/geo-map.tsx` as a `"use client"` component. The MapLibre library + its CSS are dynamic-imported inside `useEffect`, so the bundle only loads when an artifact actually mounts the geo-map. Style is a hand-rolled raster style pointing at OpenStreetMap tiles (no Mapbox token requirement). Topology resolution mirrors `network-graph.tsx` — inline `view_spec.{buses,lines}` first, otherwise lazy-fetched from `/api/artifacts/<id>/topology`. Layout rules: bus glyphs placed at lat/lon when every bus has `x`/`y` defined, force-directed SVG fallback otherwise (with a note explaining why the map fell back). Line styling: log-scaled thickness from `s_nom`, signed flow → diverging green→cream→red color, DC lines (carrier=="DC" or `dc===true`) get a dashed dasharray (`[2,2]` on the map's `lines-dc` layer; `4,3` on the SVG fallback). LMP overlay paints bus circles with a diverging blue→cream→red palette anchored on the dispatch's full-window LMP extent. Hover popup shows id / carrier / LMP. Time slider scrubs `dispatch.hours`; both bus colors and line colors re-paint on scrub via `map.getSource(...).setData(...)`. Wired into `components/renderers/types.ts` (added `"geo-map"` to `RendererName`) and `components/renderers/index.tsx` (new dispatch arm). Default for `kind='network'` remains the topology graph; the agent (or canonical seeds) can opt into the map via `view_spec.renderer = "geo-map"`. `npm run build` exits 0.
- 2026-05-23 — runs panel + chart view spec landed (ROADMAP §6, LOOP_QUEUE item 10). `npm install vega vega-lite vega-embed`; `components/renderers/chart.tsx` is now a client component that dynamic-imports `vega-embed` and renders a real Vega-Lite spec (autosize=fit, container width, SVG output, no action menu). It exports a reusable `<VegaLiteChart>` used by both the artifact dispatcher and the new run pages. Added `components/renderers/run.tsx` (kind='run' default renderer) which wraps `components/runs/RunView.tsx` — RunView builds three Vega-Lite specs from `view_spec.{lmps,carriers,flows}` (long-form `{t, bus|carrier|line, value}` records). Top-K-by-variance filtering keeps line/LMP charts readable on large networks. Pages: `app/app/runs/page.tsx` (RunsPanel — lists `kind='run'`, checkbox-pick two rows to compare), `app/app/runs/[id]/page.tsx` (single-run view), `app/app/runs/compare/page.tsx` (`?a=&b=` side-by-side grid). Workspace shell now routes the Runs rail item to `/app/runs` and labels the center tab accordingly. To populate the panel end-to-end: extended `scripts/smoke_dispatch.py` with a `run_dispatch()` helper that returns `(outcome, pnet, snapshots, solver, elapsed)`; `scripts/run_artifact.py` builds the run row + view_spec (LMPs from `outcome.prices`, carriers/flows from `pnet.generators_t.p` / `pnet.lines_t.p0` if present, best-effort otherwise). Both `scripts/seed_networks.py` and `scripts/ingest_pypsa_folder.py` now emit a run artifact alongside the network (canonical for seeded networks; per-user-owned, `status='draft'` for uploads). `npm run build` exits 0. Acceptance for §6.5 runs/charts/compare met; carrier and line-flow time-series only populate when the source PyPSA network ships pre-solved snapshots — zap-only dispatch fills LMPs and the agent can author richer run artifacts.
- 2026-05-23 — PDF source ingestion + glossary + context doc landed (ROADMAP §1/§3, LOOP_QUEUE item 11). Schema: new `public.source_chunks(id, artifact_id, user_id, ordinal, page, text, embedding jsonb, metadata, created_at)` with own-row RLS and indexes on `(artifact_id, ordinal)` and `user_id`. Backend: `app/api/upload/pdf/route.ts` accepts a PDF (multipart `file`), writes it under `<workspace>/sources/<slug>/v<N>/`, looks up prior versions by user+slug to set `parent_id` and bump `metadata.version`, creates a `source_document` artifact, and spawns `scripts/ingest_pdf.py` detached. The ingester walks the pipeline (`queued → extracting → chunking → embedded → ready`): pypdf extracts per-page text, paragraphs are chunked at ~1200 chars with 200-char overlap, each chunk gets a 128-dim hashing-based bag-of-words embedding (deterministic, no ML deps — upgradeable later), batch-inserted via Supabase REST. Heuristic extraction pulls (a) acronyms in the "Full Form (ACR)" pattern, (b) definition-style sentences ("X is defined as Y", "X refers to Y", "X is a Y") with sentence-boundary anchoring so terms don't slurp the prior sentence's tail. New terms merge into `<workspace>/glossary.md` (markdown body with per-term source attribution); the narrative blurb (title + lead paragraph + section headings) merges into `<workspace>/company-context.md` keyed by source label so re-runs don't duplicate. When the artifact has a `parent_id`, the script reads the parent's `metadata.extracted_text` and emits a `view_spec.renderer="diff"` with `before`/`after`. Workspace bootstrap (`lib/user-workspace.ts`) now writes `instructions: ["glossary.md", "company-context.md"]` into `.opencode/opencode.jsonc` and `writeIfMissing`s stub copies of both files; opencode's `session/instruction.ts` resolves those via `globUp` from the workspace cwd, so they're injected into the system prompt every session (verified by reading `prompt.ts:1419-1425` — `[...env, ...instructions, ...skills]`). New routes: `/app/sources` (`components/sources/SourcesPanel.tsx` — drag-drop a PDF, polls status, links to `/app/artifacts/<id>`); `/app/glossary` (server component reads the live workspace `glossary.md` + `company-context.md` and renders them side-by-side via `MarkdownRenderer`). LeftRail "Sources" and "Glossary" entries now route via `RAIL_ROUTES`. **User must paste updated `supabase/schema.sql` into Supabase** before `source_chunks` inserts succeed. **Caveats**: PDFs with scanned-image-only content fail (no OCR yet, marked `failed_validation` with `pipeline_error`); the embedding is lexical/hashing-based — not semantic — so retrieval search by similarity isn't useful yet (intended as schema-stable placeholder); the heuristic glossary extractor over-fits short definitions and will miss anything written in non-English-canonical structure (the agent can refine `glossary.md` directly post-ingest). Installed `pypdf 6.12.1` into `/home/agent/zap/.venv/lib/python3.12/site-packages` (via `/usr/bin/pip --target=...` since the venv lacks pip).
- 2026-05-23 — artifacts table + universal renderer landed. Schema: `public.artifacts(id, user_id, org_id, kind, name, slug, fs_path, storage_path, metadata jsonb, view_spec jsonb, parent_id, parent_session_id, status, created_at, updated_at)` with indexes on `(user_id, created_at desc)`, `kind`, `parent_id`. RLS: select-own + canonical-shared rows (user_id null + status='canonical') for ROADMAP §0 seeded networks; insert/update/delete restricted to `auth.uid() = user_id`. `lib/artifacts.ts` exposes `createArtifact`/`getArtifact`/`listArtifacts`. `components/renderers/{markdown,table,chart,diff,code,file,log,dashboard}.tsx` + a `types.ts` carrying `rendererFor()` (dispatch by `view_spec.renderer` → `kind` fallback → `file`). `components/renderers/index.tsx` is the universal `<ArtifactRenderer>`. Chart renderer ships a tiny inline bar preview + Vega-Lite spec inspector — Vega-Lite proper wasn't pulled in yet (no chart artifact to render through it). Diff is a simple line-by-line unified diff. Routes: `app/api/artifacts/route.ts` (GET list with `kind`/`status`/`session`/`limit` query params; POST create) and `app/app/artifacts/[id]/page.tsx` (uses `getArtifact` + `<ArtifactRenderer>`). Proxy gates `/api/artifacts` alongside `/api/opencode`. Public schema copy regenerated. The user must paste the updated `supabase/schema.sql` into the SQL editor before any insert/select succeeds against `public.artifacts`.

Confirm with the user before diving in.
