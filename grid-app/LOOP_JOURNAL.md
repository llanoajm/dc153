# Loop journal — Workspaces redesign

Append-only notes per item. Visual/manual checks deferred to human review go here.

- 2026-05-28 — Item 6 (remove the right context rail) — DONE.
  - Deleted `components/shell/RightRail.tsx` (the placeholder "Context" aside — copy-only, nothing populated it) and removed its usage in `components/shell/WorkspaceShell.tsx`: dropped the `import { RightRail }` line, the `<RightRail collapsed={rightCollapsed} onToggle={…}/>` render, and the now-unused `rightCollapsed` state (the only consumer was that render). No other file in `components`/`app`/`tests` referenced it (`grep -rn RightRail components app tests` clean — the remaining hits are doc/historical notes in STATE.md / WORKSPACE_REDESIGN.md / REDESIGN_ROADMAP.md / LOOP_QUEUE.md, outside the acceptance grep's `components app` scope).
  - Reflow: `CenterTabs` is already `flex-1` and the shell row is `flex min-h-0 min-w-0`, so with the right column gone the chat/center pane simply expands to fill the freed width — no empty reserved column, no leftover collapsed 9px strip. Left rail + its collapse toggle are untouched.
  - Verification: `npx tsc --noEmit` exit 0; `npm run test:unit` 24/24 pass. Did NOT run `next build`/`dev`/`start`, a browser, or touch :3000 (guardrails). The Playwright snapshot of `/app` showing the chat centered (acceptance criterion) is DEFERRED TO HUMAN REVIEW — no spec change was needed since no test referenced the right rail.

- 2026-05-28 — Item 5 (left sidebar: {single Network, Chats, profile}) — DONE.
  - Reworked `components/shell/LeftRail.tsx` into three zones (WORKSPACE_REDESIGN §10): (1) the workspace's SINGLE network as a card at the top (eyebrow "Network" + name, "No network yet" when unset; clicking routes to /app/networks); (2) a "Chats" history list with a `+` new-chat button — fetches `/api/chats` on mount and renders titles, "No chats yet" empty state, highlights the active chat; (3) the secondary workspace sections (Sources/Networks/Runs/Glossary/Orgs/Dashboards/Panels/Settings/…) moved BELOW under a collapsed "More" disclosure so their routes still resolve (constraint: old routes must work; item 16 owns the full demotion to a Library) without competing for the top of the rail. Pinned-dashboards block kept as-is. The old `⌘K to search` footer hint was dropped to make room for the profile zone (the CommandPalette + ⌘K still work — it's still mounted in WorkspaceShell).
  - New `components/shell/ProfileMenu.tsx` ("use client"): a profile circle (user's initial avatar) pinned at the BOTTOM-LEFT of the rail. Clicking opens a popover (closes on outside-click / Escape) holding the account email, a Settings link, and the Sign out form (`POST /auth/signout`, unchanged). `aria-label="Account menu"` + `role="menu"` for testability/a11y.
  - Removed the top-right header Sign out form from `app/app/layout.tsx` (the header now carries only the lockup + OrgSwitcher) and threaded `email={user.email}` into `WorkspaceShell` → `LeftRail` → `ProfileMenu`. Logout now lives ONLY in the bottom-left profile menu.
  - Chat reopen wiring: `WorkspaceShell` now reads `?chat`/`?new` (under a `<Suspense>` boundary — split into `WorkspaceShellInner` — so a future `next build` doesn't bail out of static optimization on `useSearchParams`). New-chat → `/app?new=<Date.now()>` (unique so repeated clicks always re-bootstrap); open-chat → `/app?chat=<id>`. `app/app/page.tsx` likewise split into `ChatPage`→`ChatPageInner` under Suspense; its mount effect now branches: `?chat=<id>` fetches `GET /api/chats/[id]` for the opencode session id and adopts it (clears messages, sets `chatIdRef` so later messages PATCH-touch instead of creating a dup row), otherwise bootstraps a fresh session and resets `chatIdRef`. The existing backfill + SSE effects key off `sessionId`, so the transcript reloads automatically on reopen.
  - Single-network source: no workspace is bound yet (items 9-10), so the rail mirrors the chat composer — it resolves the active-network selection from `localStorage` (`steinmetz.activeNetworkId`) against `/api/artifacts?kind=network`. Once the gallery/wizard land, this should read the bound `workspaces.primary_network_id` instead.
  - Store/route extension (additive, for a useful history list before workspaces exist): added `listMyChatsWith(db)` to `lib/chats-store.ts` (unscoped recency list; RLS does owner-scoping) + `listMyChats()` in `lib/chats.ts`; `GET /api/chats` now lists ALL of the caller's chats when `workspace_id` is omitted (was a 400). The `ChatDbClient` structural type gained a top-level `select().order()` path.
  - Test updates (Playwright specs are the deliverable — NOT run here; can't drive a browser against prod :3000 per guardrails): `tests/flows/zz-logout.spec.ts` now asserts BOTH halves of the §5 contract — Sign out is absent from `<header>` and hidden until the bottom-left "Account menu" is opened, then exercises the redirect. Updated the other specs that assumed header-signout / flat rail buttons so they stay accurate for the next CI/human run: `auth.anon.spec.ts` (open profile menu post-login), `edge-and-a11y.spec.ts` (expand "More" before clicking Networks/Runs; profile circle is the authed marker on reload), `app-shell.spec.ts` (Network/Chats labels + "More" group + "account menu"; placeholder regex refreshed to /ask the agent|loading/), and the out-of-loop demo `tests/demos/13-logout.demo.ts`.
  - New unit test in `tests/unit/chats-store.test.mjs` for `listMyChatsWith` (cross-workspace recency, incl. an unbound workspace_id=null chat); fake DB gained the top-level `order()` path.
  - Verification: `npx tsc --noEmit` exit 0; `npm run test:unit` 24/24 pass (23 prior + 1 new). Did NOT run `next build`/`dev`/`start`, a browser, or touch :3000 (guardrails). Visual review of the new sidebar (premium feel, spacing, the profile popover) + a live Playwright run of the updated logout/shell specs are DEFERRED TO HUMAN REVIEW.
  - Human follow-up: the sidebar chat list + `/api/chats` still depend on migrations 0001+0002 being applied to the live Supabase DB (not yet done — see items 1/4 notes); until then the list is empty (the GET 500s and the rail swallows it). Two known UX gaps left for later items: (a) creating a brand-new chat's first message does not live-refresh the sidebar list (no `?chat` change to trigger the reload) — it appears on next navigation; (b) the "single network" is the localStorage active-network until item 9-11 binds a real workspace network.

- 2026-05-28 — Item 4 (chat persistence + chats store) — DONE.
  - Design choice: a dedicated `chats` TABLE, not a `kind='chat'` artifact. A chat is not a renderable artifact — it's a durable handle mapping a workspace → an opencode session id + a human title, ordered by recency for the sidebar history list (§10). opencode sessions are otherwise ephemeral with no durable list; this table IS that list. Messages stay in opencode and are reloaded by session id on reopen, so the table stores the mapping, not the transcript. The artifact columns (view_spec/kind/parent_*) don't fit a chat handle, so a table is cleaner.
  - `supabase/migrations/0002_chats.sql`: additive idempotent `chats` table (`id`, `workspace_id` FK→workspaces ON DELETE CASCADE, `user_id`/`org_id` with the same `(user_id is null) <> (org_id is null)` one-owner check used by workspaces, `session_id text not null`, `title` default 'New chat', `created_at`/`updated_at`/`last_message_at`), three indexes (`(workspace_id, last_message_at desc)`, `(user_id, last_message_at desc)`, `(session_id)`), and 4 RLS policies (select/insert/update/delete) mirroring workspaces/artifacts via `is_org_member`/`has_org_role`. Every object guarded (`create table/index if not exists`, `drop policy if exists`→`create policy`). Folded the identical block into `supabase/schema.sql` (after the artifacts.workspace_id index, so the workspaces/orgs FK + helper deps already exist) so a fresh paste is complete.
  - Parse-check: `pglast` 7.13 (libpg_query) — migration parses (13 statements), schema.sql parses (135), and schema.sql + 0002 concatenated parses (148), confirming the migration is re-pasteable on top. NOT applied to the live DB (no local pg server on the VM; same as items 1-2).
  - Store layer split so it's unit-testable: `lib/chats-store.ts` holds the pure/injectable core (NO `server-only`/`@/` imports so jiti can load it) — `createChatWith`/`listChatsWith`/`getChatWith`/`touchChatWith` against a minimal `ChatDbClient` structural type, plus pure `deriveChatTitle` (collapses whitespace, strips the composer's `[active-network]` preamble, truncates at 80). `lib/chats.ts` re-exports the core types + `deriveChatTitle` and adds the RLS-checked `createClient()`-backed wrappers (`createChat`/`listChats`/`getChat`/`touchChat`) the routes call.
  - Routes: `app/api/chats/route.ts` — `GET ?workspace_id=` lists a workspace's chats (recency desc), `POST {workspace_id, session_id, first_message?, title?, org_id?}` creates one. `app/api/chats/[id]/route.ts` — `GET` returns one chat (its session id is the reopen handle; client reloads messages via the existing `GET /api/opencode/session/[session_id]/message`), `PATCH` bumps recency. `/api/chats` gated in `proxy.ts`.
  - Front-end wiring (`app/app/page.tsx`): added `chatIdRef`; on a successful prompt POST, `persistChat(session, rawMessage)` POSTs `/api/chats` on the first message (storing workspace_id=null until the gallery/wizard items 9-10 bind a workspace — the column is nullable by design) and PATCHes recency on later ones. Best-effort: a persistence hiccup never breaks the conversation (which lives in opencode). The sidebar that LISTS/OPENS chats from this store is item 5 — this item delivers the store + persistence + reopen handle.
  - Verification: `npx tsc --noEmit` exit 0; `npm run test:unit` 23/23 pass (13 prior + 10 new in `tests/unit/chats-store.test.mjs` covering deriveChatTitle, create persists with derived title, org-owned nulls user_id, list recency-desc + workspace-scoped, reopen via getChat returns session id, getChat null for unknown, touch floats a chat up the list). Did NOT run a browser / `next build`/`dev` / touch :3000 per guardrails. Visual review of the eventual sidebar UI is deferred to item 5; nothing visual changed in this item beyond the silent persist call.
  - Human follow-up: migrations 0001 AND 0002 are NOT applied to the live Supabase DB. A person must paste `supabase/migrations/0001_workspaces.sql` then `0002_chats.sql` (or the updated `supabase/schema.sql`) into the SQL editor before the `chats` table is live. Until then POST/GET `/api/chats` return a 500 from the missing table, but `persistChat` is best-effort so the chat UI keeps working.

- 2026-05-28 — Item 3 (ensureWorkspace(workspaceId) + per-workspace dir) — DONE.
  - Extracted the directory-keyed bootstrap out of `lib/user-workspace.ts:ensureUserWorkspace(userId)` into a new exported `materializeWorkspaceDir(dir)` (creates `.opencode/{agent,skills}`, `features/`, `.python_libs/`, the per-user venv, and writes the stub AGENTS.md / grid-engineer persona / glossary / company-context / example feature + the opencode config via the existing `writeOpencodeConfig`). `ensureUserWorkspace` now just computes `dir = ROOT/<uid>`, calls `materializeWorkspaceDir(dir)`, then keeps its per-USER hardening (`ensureLinuxAccount(dir,userId)` §3.1 + `ensureUserSlice(userId)` §3.4) and returns `dir` — behavior unchanged for every existing caller (back-compat shim preserved). The handed-off-dir short-circuit is untouched.
  - New `lib/workspace.ts` exports `ensureWorkspace(workspaceId)` → `grid-workspaces/<workspace-id>/`, reusing `materializeWorkspaceDir` so the per-workspace layout is identical to a per-user one. It records the workspace's primary network in `.steinmetz/workspace.json` (REDESIGN §4.4) via `readWorkspaceMeta` / `writeWorkspaceMeta` / `setPrimaryNetwork(workspaceId, networkId, name?)`; meta carries `{workspace_id, primary_network_id, primary_network_name, updated_at}`. First materialization seeds meta without clobbering an existing file (so a re-materialization preserves an already-anchored network); a drifted `workspace_id` self-heals. Also exports `workspaceDirFor(workspaceId)`. NOTE: per-workspace hardening (Linux account / slice re-keyed from user→workspace owner per §4.4) is intentionally NOT wired here yet — the existing gates (`STEINMETZ_*`) are off in dev and re-keying hardening is its own follow-up; `ensureWorkspace` deliberately does only the dir bootstrap + meta.
  - Threaded `workspaceId` through `lib/opencode-client.ts`: added `resolveWorkspaceDir(userId, workspaceId?)` (→ `ensureWorkspace(workspaceId)` when set, else legacy `ensureUserWorkspace(userId)`) and an `OpencodeCallOptions { workspaceId? }` accepted by `createSession` / `getMessages` / `abortSession`, plus `workspaceId?` on `sendPrompt`'s opts. When a `workspaceId` is passed the opencode session opens with `cwd` = the per-workspace dir; `userId` is still required for transport routing + provider-key resolution. No call site passes `workspaceId` yet, so the live per-user flow is byte-identical — the param is plumbing for the gallery/wizard items (9-10) to use. No import cycle: opencode-client → workspace → user-workspace (one direction).
  - Verification: `npx tsc --noEmit` exit 0 (existing per-user path still compiles); `npm run test:unit` 13/13 pass. Did NOT run a browser / touch :3000 / run any next build per guardrails — no UI in this item, so nothing visual to review.

- 2026-05-28 — Item 2 (backfill script: default workspace per user) — DONE.
  - Added `scripts/backfill_workspaces.py` (stdlib only — `urllib`/`json`/`argparse`; same `.env.local` loader + service-role PostgREST pattern as `scripts/seed_networks.py`, no new deps). Per user that owns artifacts and/or features: ensures a personal `workspaces` row named "My workspace" (reused if one already exists, keyed by `(user_id, name)`), then PATCHes `workspace_id` onto that user's artifacts that still have it NULL. Canonical/template networks (`user_id IS NULL`) never enter the per-user loop, so they stay at `workspace_id NULL` (§4.2). `--dry-run` is the DEFAULT and writes nothing; real writes require an explicit `--apply` flag for a human. Paginates GETs (Range header) so >1000-row tables don't truncate.
  - Scope note on "features": migration 0001 added `workspace_id` to `artifacts` only, NOT to the legacy `public.features` table. Stamping it there would need unreviewed DDL (guardrail-forbidden), so features are not column-updated; the redesign surfaces them as `kind='feature'`/`kind='objective'` artifacts which DO get stamped. Users owning only features still get a default workspace created. This is the only safe reading of "sets workspace_id on artifacts/features" under the guardrails.
  - Verification: `python3 -m py_compile` OK. Live-DB `--dry-run` exits 0 — clean no-op (`[plan] no users own artifacts or features`) because the live DB currently has zero artifact/feature owners. No-arg invocation also defaults to dry-run and exits 0. Logic exercised offline against a simulated dataset (mock of `_paged_get`/`_supabase_request`): asserted create-per-user, reuse-existing (no duplicate), stamp only NULL artifacts, leave canonical NULL, users-with-only-features get a workspace, and a second dry-run reports 0 changes (idempotent). All assertions passed.
  - `npx tsc --noEmit` exit 0; `npm run test:unit` 13/13 pass. No TS added this item.
  - Human follow-up: migration 0001 is NOT yet applied to the live Supabase DB — probe confirmed `public.workspaces` → 404 and `artifacts.workspace_id` → 400 "column does not exist". A person must paste `supabase/migrations/0001_workspaces.sql` (or the updated `supabase/schema.sql`) into the SQL editor, THEN run `python3 scripts/backfill_workspaces.py --dry-run` to review the real per-user plan, then `--apply`. No UI in this item, so no visual review pending.

- 2026-05-28 — Item 1 (additive schema migration: workspaces table + artifacts.workspace_id) — DONE.
  - Added `supabase/migrations/0001_workspaces.sql`: `workspaces` table (verbatim from WORKSPACE_REDESIGN.md §4.1, incl. the `check ((user_id is null) <> (org_id is null))` one-owner constraint, `focus text[]`, FKs to `auth.users`/`public.orgs`/`public.artifacts`), the two `(user_id|org_id, created_at desc)` indexes, all 4 RLS policies (select/insert/update/delete, mirroring artifacts via `is_org_member`/`has_org_role`), plus `artifacts.workspace_id` nullable column (§4.2) + `artifacts_workspace_created_idx`. Every object guarded (`create table/index if not exists`, `add column if not exists`, `drop policy if exists` → `create policy`) so it re-pastes cleanly on top of `schema.sql`.
  - Folded the identical objects into `supabase/schema.sql` (appended after `tool_runs`, so the `public.orgs`/`public.artifacts`/`is_org_member`/`has_org_role` FK + helper dependencies already exist) — a fresh paste is now complete.
  - Parse-check: no local postgres server or `initdb`/`postgres` binary on this VM (only the `psql` client), so a `psql -f` against a throwaway pg wasn't possible. Used `pglast` 7.13 (libpg_query — the actual PostgreSQL C parser) installed to a throwaway dir `/tmp/sqlparse-check`. Both files parse (migration: 14 statements; schema.sql: 122). The schema.sql+migration concatenation parses (136 statements), confirming the migration is re-pasteable on top. NOT applied to the live Supabase DB.
  - Verification: `npx tsc --noEmit` exit 0; `npm run test:unit` 13/13 pass. No TS added this item.
  - Human follow-up: a person must paste the migration (or the updated `schema.sql`) into the Supabase SQL editor before the `workspaces` table / `artifacts.workspace_id` are live. No UI in this item, so no visual review pending.
