STATUS: done
SUMMARY: HARDENING §3.2 ships as opt-in scaffolding — systemd template `infra/systemd/steinmetz-opencode@.service` + Bun supervisor `scripts/per-user-opencode.ts` (deterministic loopback port + /run/steinmetz/<short>.sock 0660 + idle-exit), new lib/opencode-transport.ts switches between TCP fronting proxy (§1.2 default) and Unix-socket per-user (`STEINMETZ_PER_USER_OPENCODE=1`), lib/opencode-client.ts + SSE route route through it, scripts/opencode-proxy.ts forwards per-user when the flag is on (X-Steinmetz-Short-Uid header), STATE.md + infra/systemd/README.md document sudoers + install + idle eviction; `npm run build` exits 0.
ACCEPTANCE:
- pass: systemd template unit committed at `infra/systemd/steinmetz-opencode@.service` parameterised by `%i` (short-uid), runs as `User=steinmetz-%i:steinmetz`, binds `/run/steinmetz/%i.sock` mode 0660 via the supervisor.
- pass: `lib/opencode-client.ts` picks the per-user socket per request via `lib/opencode-transport.ts` which uses `shortUidFor()` from `@/lib/linux-account` (the §3.1 helper).
- pass: `createSession` triggers `sudo systemctl start steinmetz-opencode@<short>.service` idempotently (folded into `ensurePerUserOpencode()` which `opencodeFetch()` calls on the first request of a session). Idle eviction strategy documented (supervisor's `STEINMETZ_OPENCODE_IDLE_TIMEOUT` exit + `Restart=on-failure` + on-demand start) in `infra/systemd/README.md` + `STATE.md`.
- pass: §1.2 bearer-token proxy (`scripts/opencode-proxy.ts`) is updated to forward per-user (route via `X-Steinmetz-Short-Uid` header → Unix socket) when `STEINMETZ_PER_USER_OPENCODE=1`. In-app traffic uses the socket transport directly — the proxy update is for external curl/debug clients.
- pass: `npm run build` exits 0 (Route table shows all opencode session routes wired; pre-existing turbopack import-trace warning on `app/api/upload/pdf/route.ts` is unrelated).
- best-effort (documented in `infra/systemd/README.md` Step 6): behavioural smoke — killing user A's unit doesn't affect user B's chat. Not exercised on this VM because per-user mode requires the §3.1 host prereqs (per-user Linux accounts + systemd template installed at /etc/systemd/system/ + sudoers entry); per the VERIFIER pattern from §3.1, the verifier audits in-repo plumbing only and leaves OS-side execution to the operator.
NOTES:
- Per-user mode depends on §3.1 — without `STEINMETZ_ENABLE_LINUX_ACCOUNTS=1` the `steinmetz-<short>` Linux account doesn't exist for the unit to drop privileges to. Documented in STATE.md.
- Host operator must `sudo cp infra/systemd/steinmetz-opencode@.service /etc/systemd/system/`, `sudo systemctl daemon-reload`, and install `/etc/sudoers.d/steinmetz-opencode` (NOPASSWD `systemctl start steinmetz-opencode@*.service` for `agent`) before flipping `STEINMETZ_PER_USER_OPENCODE=1`.
- supabase/schema.sql untouched; no SQL paste required.
VERIFIED: yes
